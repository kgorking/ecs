name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Create build dir
      run: cmake -S . -B build -G Ninja
    - name: Run ninja
      run: ninja -C build
